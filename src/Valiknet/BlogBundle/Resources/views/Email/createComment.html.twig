Коричувач <a href="{{ path('account_index_page', {"slug" : authorComment.slug}) }}">{{ authorComment.nickname }}</a> додав комментар до вашої статі <a href="{{ path('view_post', {"slug" : post.slugPost}) }}">{{ post.title }}</a>